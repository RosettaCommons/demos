-s input/1AWR.ex.ppk.pdb     # prepacked structure will be the input structure for refinement
-native input/1AWR.pdb       # the native structure, for RMSD calculations. If it is not given, the starting structure is used as reference
-pep_refine                  # The flag that allows peptide refinement 
-ex1 
-ex2aro 
#-unboundrot input/1AWR.pdb  # Add position-sepcific rotamers to the rotamer library (most useful for including rotamers of unbound receptor - this example 
                             # uses the bound receptor, so information about the final structure is excluded from the run)   
#-use_input_sc               # Add the starting structure's rotamers to the rotamer library (not used for the same reason)
-nstruct 1                   # In the demo only one structure will be created.
                             # In real case scenarios it's recommended to create 100 structures with an additional low resolution step 
                             # and 100 structures in which only a high resolution step is performed
-lowres_preoptimize          # This is the low resolution optional optimization step
-flexpep_score_only          # add aditional interesting scores to scorefile
#-peptide_anchor 166         # optional flag - manually choose the anchor by which the peptide and receptor are connected
